apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: orborus-claim0
  name: orborus-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
