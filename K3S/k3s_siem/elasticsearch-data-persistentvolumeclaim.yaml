apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: elasticsearch-data
  name: elasticsearch-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
