apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: filebeat-var
  name: filebeat-var
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
